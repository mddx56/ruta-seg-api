meta {
  name: Cambiar estado de tarifa de ruta
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/route-fares/ROUTE_FARE_ID_AQUI/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
