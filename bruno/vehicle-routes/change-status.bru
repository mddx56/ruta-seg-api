meta {
  name: Cambiar estado de asignación micro-ruta
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/vehicle-routes/VEHICLE_ROUTE_ID_AQUI/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
