APP_NAME=MotosGo
IS_LOGGER=true

DB_HOST=localhost
DB_USER=postgres
DB_PASS=654321
DB_NAME=motosgo
DB_PORT=5432

NGINX_PORT=80
GOLANG_PORT=8888
APP_ENV=localhost

GRPC_PORT=50051

JWT_SECRET=<your secret key>

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SENDER_NAME="Go.Gin.Template <no-reply@testing.com>"
SMTP_AUTH_EMAIL=<your email>
SMTP_AUTH_PASSWORD=<your password>
API_KEY=dd9sad709asyd8y

GOOGLE_CLIENT_ID=<your google client id>
GOOGLE_CLIENT_SECRET=<your google client secret>

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=<your redis password>

# Push notifications (módulo micros, Fase 4) — opcional: si no se configura ninguna,
# las notificaciones siguen funcionando por WebSocket, solo no se envía push al celular.
# Usar UNA de las dos (el JSON tiene prioridad si ambas están presentes):
FCM_CREDENTIALS_FILE=<ruta al JSON de la cuenta de servicio de Firebase>
FCM_CREDENTIALS_JSON=<contenido JSON de la cuenta de servicio, en una sola línea>

DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=root
DB_NAME=motos
DB_PORT=5432
DATABASE_LOCAL=postgresql://postgres:root@127.0.0.1:5432/motos